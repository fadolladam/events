# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Dependencies & build output (also covered by frontend/ and backend/ .gitignore)
node_modules/
dist/
# backend/vendor/ is intentionally committed so the app runs on a host with no Composer.

# Environment / secrets
.env
.env.*
!.env.example
!.env.xampp
!.env.docker.example

# Local databases
*.sqlite
*.sqlite-journal

# Logs
*.log
