# Real participant data must not be committed. Only docs are tracked — no
# filename is whitelisted here, since a whitelisted name is exactly what
# lets someone's real import silently become trackable the moment they save
# their real data over the old template at that same path.
*
!.gitignore
!README.md
